# Simba backend — re-wired auf energietools.
#
# Rechenkern: energietools (MIT, aus dem Monorepo). Im Container via
#   pip install -e /repo            # installiert das energietools-Paket
# bzw. lokal ist es bereits editierbar installiert. energietools zieht seine
# eigenen Abhängigkeiten (pydantic, pandas, numpy, ...).
#
# Hybrid/Beschaffung: die Live-Connectoren (aWATTar-Spot, ENTSO-E-Balancing,
# Solis, PVGIS-Stundenserie) + die volle Wärmepumpen-Summary laufen weiter über
# das vendored ``pvtool`` (apps/simba/vendor/pvtool) und werden LAZY importiert —
# ohne pvtool bootet die App, nur diese Endpunkte sind dann nicht verfügbar.
fastapi>=0.115
uvicorn[standard]>=0.34
python-multipart>=0.0.18
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
pandas>=2.0
numpy>=1.24
