# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
*.whl

# Virtual environments
.venv/
venv/
env/
ENV/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Ruff / linters
.ruff_cache/

# IDEs
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.*
!.env.example

# Data / secrets
# data/ is a runtime/output dir (kept via data/.gitkeep). Packaged data lives in
# energietools/data/ — never duplicate it here.
data/*.db
data/*.sqlite
data/*.json
*.pem
*.key

# Internal surveys / scratch notes — never accidentally track these (may carry
# third-party repo names, private paths or unlicensed externals).
*_SURVEY.md
PVTOOL_INTEGRATION_SURVEY.md

# Generated demo gallery (built by demos/build_site.py / CI)
/site/

# marimo cache
__marimo__/
